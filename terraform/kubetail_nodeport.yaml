apiVersion: v1
kind: Service
metadata:
  name: kubetail-expose
  namespace: kiratech-test
spec:
  selector:
    app.kubernetes.io/component: dashboard
    app.kubernetes.io/name: kubetail
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
